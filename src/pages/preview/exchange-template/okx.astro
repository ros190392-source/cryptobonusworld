---
/*
 * PREVIEW ONLY — noindex, not linked from anywhere.
 * Renders OKX exchange page via the reusable ExchangePromoPage template.
 * POC for the exchange page factory approach.
 *
 * PENDING before live route:
 *   - wordmarkImg: dedicated OKX wordmark needed
 *   - articleImg:  1200×675 article banner needed
 *   - heroBackgroundImg: OKX hero background image needed
 *   - offers.ts entry for OKX
 *
 * Live page:    /okx/  (NOT YET CREATED — do not create without owner approval)
 * Preview page: /preview/exchange-template/okx/  ← you are here
 */
import CleanLayout from '../../../layouts/CleanLayout.astro';
import ExchangePromoPage from '../../../components/exchange/ExchangePromoPage.astro';
import { okxConfig } from '../../../data/exchangePages/okx';
---

<CleanLayout
  title={okxConfig.pageTitle}
  description={okxConfig.pageDescription}
  canonical={okxConfig.canonicalUrl}
  ogImage={okxConfig.ogImage}
  ogTitle={okxConfig.ogTitle}
  ogDescription={okxConfig.ogDescription}
  noindex={true}
>
  <ExchangePromoPage config={okxConfig} />
</CleanLayout>
