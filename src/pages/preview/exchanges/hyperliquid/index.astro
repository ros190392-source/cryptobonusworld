---
// PREVIEW ONLY — noindex, excluded from sitemap. Batch 01.
import CleanLayout from '../../../../layouts/CleanLayout.astro';
import ExchangeReviewPreviewPage from '../../../../components/exchange-preview/ExchangeReviewPreviewPage.astro';
import { batch01 } from '../../../../data/exchangePreview/batch-01';

const entry = batch01.find(e => e.slug === 'hyperliquid')!;
---

<CleanLayout
  title="Hyperliquid Referral Code: Preview Status | CryptoBonusWorld"
  description="Preview the current CryptoBonusWorld review status for Hyperliquid. No verified referral code or bonus is currently listed."
  canonical={`https://cryptobonusworld.com${entry.pageRoute}`}
  noindex={true}
>
  <ExchangeReviewPreviewPage entry={entry} />
</CleanLayout>
